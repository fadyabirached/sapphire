DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=SapphireDB

JWT_SECRET=replace-with-a-long-random-secret

HF_TOKEN=replace-with-your-huggingface-token
HF_MODEL=Falconsai/nsfw_image_detection

COHERE_API_KEY=replace-with-your-cohere-api-key

# Admin login for the Admin dashboard (POST /admin/login).
# Generate the hash with:
#   node -e "console.log(require('bcrypt').hashSync('your-password', 10))"
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD_HASH=replace-with-a-bcrypt-hash
